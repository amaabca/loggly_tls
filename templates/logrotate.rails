# {{ ansible_managed }}
/var/log/rails/*.log {
  su root root
  daily
  rotate 3
  missingok
  notifempty
  delaycompress
  sharedscripts
  postrotate
    service rsyslog stop
    rm /var/spool/rsyslog/imfile-state*
    service rsyslog start
  endscript
}
