$ModLoad imfile
$InputFilePollInterval 10
$PrivDropToGroup adm

$template LogglyFormat{{ loggly.application.tag }},"<%pri%>%protocol-version% %timestamp:::date-rfc3339% %HOSTNAME% %app-name% %procid% %msgid% [{{ loggly.token }}@41058 tag=\"{{ loggly.application.tag }}\"] %msg%\n"

# Setup disk assisted queues
$WorkDirectory /var/spool/rsyslog # where to place spool files
$ActionQueueFileName fwdRule1     # unique name prefix for spool files
$ActionQueueMaxDiskSpace 1g       # 1gb space limit (use as much as possible)
$ActionQueueSaveOnShutdown on     # save messages to disk on shutdown
$ActionQueueType LinkedList       # run asynchronously
$ActionResumeRetryCount -1        # infinite retries if host is down

#RsyslogGnuTLS
$DefaultNetstreamDriverCAFile {{ loggly.certificate_dest }}{{ loggly.certificate_file }}
$ActionSendStreamDriver gtls
$ActionSendStreamDriverMode 1
$ActionSendStreamDriverAuthMode x509/name
$ActionSendStreamDriverPermittedPeer *.loggly.com


# Application files
{% for filename in loggly.application.logs  %}
$InputFileName {{ filename }}
$InputFileTag {{ loggly.application.tag }}:
$InputFileStateFile stat-{{ loggly.application.tag }}{{ loop.index }}
$InputFileSeverity info
$InputFilePersistStateInterval 20000
$InputRunFileMonitor

{% endfor %}


*.* @@logs-01.loggly.com:6514;LogglyFormat{{ loggly.application.tag }}
*.* ~
